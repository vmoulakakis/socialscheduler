import base from './worker-v6.js';

const PATCH = String.raw`
<style id="ss-v7-style">
#autopilot-actual{margin:0 0 16px;position:relative;z-index:3}.aa-wrap{background:linear-gradient(180deg,rgba(14,34,53,.98),rgba(7,20,34,.99));border:1px solid #2a4865;border-radius:18px;box-shadow:0 22px 70px rgba(0,0,0,.26);overflow:hidden}.aa-head{display:flex;justify-content:space-between;gap:14px;align-items:center;padding:17px 18px;border-bottom:1px solid #203a54}.aa-head h2{margin:0;font-size:18px}.aa-head p{margin:3px 0 0;color:#91a7bc;font-size:11px}.aa-live{display:inline-flex;align-items:center;gap:7px;border:1px solid #2b7558;background:#0b2b21;color:#75e7b1;padding:6px 9px;border-radius:999px;font-size:10px;font-weight:800}.aa-dot{width:8px;height:8px;border-radius:50%;background:#48dca0;box-shadow:0 0 0 5px rgba(72,220,160,.08)}.aa-grid{display:grid;grid-template-columns:minmax(0,1fr) 310px;gap:0}.aa-map{padding:16px;min-width:0}.aa-nodes{display:grid;grid-template-columns:repeat(8,minmax(118px,1fr));gap:8px;overflow:auto;padding-bottom:5px}.aa-node{position:relative;min-width:118px;background:#091a2a;border:1px solid #25435e;border-radius:13px;padding:11px;color:#eef7ff;text-align:left;cursor:pointer;transition:.16s ease}.aa-node:hover,.aa-node.on{transform:translateY(-2px);border-color:#68e3f5;background:#0d2536}.aa-node:after{content:'→';position:absolute;right:-8px;top:43%;color:#55728c;z-index:4}.aa-node:last-child:after{display:none}.aa-ico{font-size:19px}.aa-name{font-size:10px;font-weight:900;text-transform:uppercase;letter-spacing:.45px;margin-top:5px}.aa-big{font-size:22px;font-weight:950;margin-top:4px}.aa-sub{font-size:8.5px;color:#91a7bc;line-height:1.3;min-height:24px}.aa-track{height:5px;background:#06121f;border-radius:999px;overflow:hidden;margin-top:7px}.aa-track span{display:block;height:100%;background:linear-gradient(90deg,#68e3f5,#a391ff)}.aa-detail{margin-top:10px;border:1px solid #24435e;background:#081724;border-radius:12px;padding:12px}.aa-detail h3{font-size:13px;margin:0 0 4px}.aa-detail p{margin:0;color:#91a7bc;font-size:10px}.aa-mini-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:7px;margin-top:9px}.aa-mini{border:1px solid #213c56;background:#0b1d2e;border-radius:9px;padding:8px}.aa-mini b{display:block;font-size:12px}.aa-mini small{display:block;color:#8ea6bd;font-size:8.5px;margin-top:2px}.aa-brief{border-left:1px solid #203a54;padding:16px;background:linear-gradient(180deg,rgba(13,31,49,.85),rgba(8,21,34,.85))}.aa-brief h3{margin:0 0 9px;font-size:14px}.aa-row{display:flex;gap:9px;padding:9px 0;border-bottom:1px solid #1b334a}.aa-row:last-child{border-bottom:0}.aa-badge{flex:0 0 27px;width:27px;height:27px;border-radius:9px;background:#132e46;display:grid;place-items:center;font-size:10px;font-weight:900}.aa-row b{display:block;font-size:10.5px}.aa-row small{display:block;color:#91a7bc;font-size:9px;margin-top:2px}.aa-routes{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:7px;margin-top:11px}.aa-route{border:1px solid #24425d;border-radius:9px;padding:8px;background:#0a1a2a}.aa-route .p{text-transform:capitalize;font-size:9px;color:#91a7bc}.aa-route b{font-size:11px}.aa-route small{display:block;color:#7590a9;font-size:8px}.aa-note{margin-top:9px;color:#6f8aa4;font-size:8.5px}.aa-error{padding:12px 16px;color:#ff9da8;font-size:10px}.aa-loading{padding:13px 16px;color:#91a7bc;font-size:10px}
@media(max-width:1250px){.aa-grid{grid-template-columns:1fr}.aa-brief{border-left:0;border-top:1px solid #203a54}.aa-nodes{grid-template-columns:repeat(8,135px)}.aa-mini-grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:700px){.aa-head{align-items:flex-start}.aa-routes{grid-template-columns:repeat(2,1fr)}.aa-mini-grid{grid-template-columns:1fr}}
</style>
<script id="ss-v7-script">
(function(){
  var SB='https://rpfadpdnnxequgvdcfoq.supabase.co',KEY='sb_publishable_NkMSCtURWbZcA8MCY1H5sA_W_G10WYD',ADMIN=SB+'/functions/v1/socialscheduler-admin-api',ORCH=SB+'/functions/v1/socialscheduler-orchestrator-api';
  var data=null,plus=null,selected='seo',loading=false,last=0;
  function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]})}
  function n(v){return Number(v||0)}function pct(a,b){return b?Math.max(0,Math.min(100,Math.round(a/b*100))):100}
  function sess(){try{return JSON.parse(localStorage.getItem('ss_session')||'null')}catch(e){return null}}
  async function call(url,action){var s=sess();if(!s||!s.access_token)throw new Error('admin_session_required');var r=await fetch(url,{method:'POST',headers:{apikey:KEY,authorization:'Bearer '+s.access_token,'content-type':'application/json'},body:JSON.stringify({action:action})});var j=await r.json().catch(function(){return{error:'invalid_response'}});if(!r.ok||j.ok===false)throw new Error(j.error||('HTTP '+r.status));return j}
  function showForView(){var t=document.querySelector('#title');return t&&(/AI Command Center|Decision Brief/.test(t.textContent||''))}
  function host(){var h=document.querySelector('#autopilot-actual');if(!h){h=document.createElement('div');h.id='autopilot-actual';var c=document.querySelector('#content');if(c&&c.parentNode)c.parentNode.insertBefore(h,c)}return h}
  function winner(p){var rows=(plus&&plus.provider_scores||[]).filter(function(x){return x.platform===p&&n(x.total_score)>0});return rows[0]||null}
  function node(id,ico,name,big,sub,coverage){return '<button class="aa-node '+(selected===id?'on':'')+'" data-aa-node="'+id+'"><div class="aa-ico">'+ico+'</div><div class="aa-name">'+esc(name)+'</div><div class="aa-big">'+esc(big)+'</div><div class="aa-sub">'+esc(sub)+'</div><div class="aa-track"><span style="width:'+Math.max(2,coverage)+'%"></span></div></button>'}
  function minis(rows){return '<div class="aa-mini-grid">'+rows.map(function(x){return '<div class="aa-mini"><b>'+esc(x[0])+'</b><small>'+esc(x[1])+'</small></div>'}).join('')+'</div>'}
  function detail(){var s=data.summary||{},a=data.autopilot_state||{},approved=n(s.approved_jobs),routes=['facebook','instagram','tiktok','linkedin'];
    if(selected==='source'){var src=plus&&plus.sources||[];return '<h3>Source truth</h3><p>CSV is the high-priority intake; SocialMarket is the native source. Priority never bypasses validation or dedupe.</p>'+minis(src.map(function(x){return[x.display_name,'priority '+n(x.priority)+' · '+n(x.ready_rows)+' ready / '+n(x.content_rows)+' rows']}))}
    if(selected==='seo')return '<h3>SEO & shareability preflight</h3><p>Every approved item must receive either AI optimization or a deterministic social-discoverability fallback. A timeout does not stop publishing.</p>'+minis([['AI optimized',n(a.seo_ai_optimized)+' posts'],['Rules optimized',n(a.seo_rules_optimized)+' posts'],['Pending',n(a.seo_pending)+' posts'],['Coverage',n(a.seo_coverage_pct)+'% of current approved']]);
    if(selected==='creative')return '<h3>Creative readiness</h3><p>Source/bulk media is reused when available. Text-capable channels may publish text-only. Instagram/TikTok missing mandatory media are held as needs-Canva instead of failing silently.</p>'+minis([['Media ready',n(a.creative_media_ready)+' posts'],['Text ready',n(a.creative_text_ready)+' posts'],['Needs Canva',n(a.creative_needs_canva)+' posts'],['Bulk assets',n(a.creative_assets)+' files']]);
    if(selected==='schedule')return '<h3>Conversion-aware scheduling</h3><p>Slots are collision-safe in Europe/Athens. Observed clicks/shares/saves/reach influence timing when evidence exists; otherwise platform priors are used.</p>'+minis([['Optimized',n(a.schedule_optimized)+' / '+approved],['Coverage',n(a.schedule_coverage_pct)+'%'],['Policy','conversion-learning-v3'],['Telemetry',n(s.metric_rows)+' measured delivery rows']]);
    if(selected==='route')return '<h3>Provider routing</h3><p>Verified accounts and provider health are hard gates. Fit, history and available capacity determine the winner; load can shift new jobs to the next eligible provider.</p>'+minis(routes.map(function(p){var w=winner(p);return[p,w?w.provider_key+' · score '+Math.round(n(w.total_score)):'no safe route']}));
    if(selected==='publish')return '<h3>Publishing evidence</h3><p>Scheduled means a publisher accepted the job. Published means delivery evidence exists; the UI does not convert scheduled into published.</p>'+minis([['Approved',n(s.approved_jobs)+' waiting'],['Scheduled',n(s.scheduled_jobs)+' accepted'],['Published',n(s.published_jobs)+' confirmed'],['Failed',n(s.failed_jobs)+' visible']]);
    if(selected==='recovery')return '<h3>Self-healing</h3><p>Failures are classified, then retried or rerouted only when a verified safe route exists. No-safe-route cases are held and remain visible.</p>'+minis([['Recovery 24h',n(a.recovery_events_24h)+' events'],['AI diagnosis 24h',n(a.ai_diagnostics_24h)+' events'],['Cadence','recovery every 5 min'],['Attempts','maximum 3']]);
    return '<h3>Learning loop</h3><p>Only observed provider metrics are used. Sparse telemetry is labeled sparse; it is never converted into invented conversion or revenue.</p>'+minis([['Metric rows',n(s.metric_rows)],['Published',n(s.published_jobs)],['Evidence strength',n(s.metric_rows)?'learning active':'prior-based'],['Next','more telemetry → stronger timing/provider weights']]);
  }
  function brief(){var s=data.summary||{},a=data.autopilot_state||{},approved=n(s.approved_jobs),seoPending=n(a.seo_pending),creativeGap=n(a.creative_needs_canva),assigned=n(a.provider_assigned),schedule=n(a.schedule_optimized),recovery=n(a.recovery_events_24h);var state=(assigned>=approved&&schedule>=approved)?'Execution fabric assigned':'Planning still filling';var next=seoPending?seoPending+' new posts are waiting for the next 10-minute SEO preflight.':creativeGap?creativeGap+' posts need a generated/supplied visual before execution.':approved+' approved posts are fully routed; publishers claim them by provider capacity.';var risk=creativeGap?'Creative media gap is blocking media-required channels.':n(s.metric_rows)===0?'Performance telemetry is sparse; conversion timing is mostly prior-based.':recovery?'Recovery actions occurred in the last 24h; inspect the recovery ledger.':'No critical autonomous blocker is visible in current counters.';return '<h3>Autopilot briefing</h3><div class="aa-row"><span class="aa-badge">✓</span><div><b>'+esc(state)+'</b><small>'+n(a.provider_assignment_pct)+'% provider assignment · '+n(a.schedule_coverage_pct)+'% schedule coverage</small></div></div><div class="aa-row"><span class="aa-badge">→</span><div><b>Next autonomous action</b><small>'+esc(next)+'</small></div></div><div class="aa-row"><span class="aa-badge">!</span><div><b>Current risk</b><small>'+esc(risk)+'</small></div></div><div class="aa-row"><span class="aa-badge">H</span><div><b>Human interaction</b><small>Optional unless credentials/OAuth, business judgment or an unresolved no-safe-route condition is required.</small></div></div>'}
  function routes(){return ['facebook','instagram','tiktok','linkedin'].map(function(p){var w=winner(p);return '<div class="aa-route"><div class="p">'+p+'</div><b>'+(w?esc(w.provider_key):'No route')+'</b><small>'+(w?'score '+Math.round(n(w.total_score))+' · health/account gated':'no verified executable provider')+'</small></div>'}).join('')}
  function render(){var h=host();if(!showForView()){h.style.display='none';return}h.style.display='block';if(loading&&!data){h.innerHTML='<div class="aa-wrap aa-loading">Loading verified Autopilot state…</div>';return}if(!data){h.innerHTML='<div class="aa-wrap aa-error">Autopilot state is temporarily unavailable. The base scheduler UI remains operational.</div>';return}var s=data.summary||{},a=data.autopilot_state||{},approved=n(s.approved_jobs),seoDone=n(a.seo_ai_optimized)+n(a.seo_rules_optimized),creativeDone=n(a.creative_media_ready)+n(a.creative_text_ready)+n(a.creative_needs_canva);var nodes='';nodes+=node('source','◈','Sources',n(s.content_items),'canonical content',100);nodes+=node('seo','✦','SEO',seoDone+'/'+approved,n(a.seo_ai_optimized)+' AI · '+n(a.seo_rules_optimized)+' fallback',pct(seoDone,approved));nodes+=node('creative','▧','Creative',creativeDone+'/'+approved,n(a.creative_needs_canva)+' need Canva',pct(creativeDone,approved));nodes+=node('schedule','◷','Schedule',n(a.schedule_optimized)+'/'+approved,'conversion slots',pct(n(a.schedule_optimized),approved));nodes+=node('route','⌁','Route',n(a.provider_assigned)+'/'+approved,'verified providers',pct(n(a.provider_assigned),approved));nodes+=node('publish','⇢','Publish',n(s.scheduled_jobs),n(s.published_jobs)+' published',pct(n(s.scheduled_jobs)+n(s.published_jobs),Math.max(1,n(s.outbox_jobs))));nodes+=node('recovery','↻','Recovery',n(a.recovery_events_24h),'events / 24h',100);nodes+=node('learn','◎','Learn',n(s.metric_rows),'measured rows',n(s.metric_rows)?100:10);h.innerHTML='<div class="aa-wrap"><div class="aa-head"><div><h2>Actual Autopilot State</h2><p>Observed production counters — click any stage for evidence and decision logic.</p></div><span class="aa-live"><span class="aa-dot"></span> AUTONOMOUS</span></div><div class="aa-grid"><div class="aa-map"><div class="aa-nodes">'+nodes+'</div><div class="aa-detail">'+detail()+'</div><div class="aa-routes">'+routes()+'</div><div class="aa-note">Observed facts are shown as counts/status. Provider/timing recommendations are decision logic, not guaranteed conversion outcomes.</div></div><aside class="aa-brief">'+brief()+'</aside></div></div>';Array.from(h.querySelectorAll('[data-aa-node]')).forEach(function(b){b.onclick=function(){selected=b.getAttribute('data-aa-node')||'seo';render()}})}
  async function load(){if(loading)return;loading=true;render();try{var r=await Promise.all([call(ADMIN,'dashboard'),call(ORCH,'dashboard_plus')]);data=r[0];plus=r[1];last=Date.now()}catch(e){console.warn('Autopilot infographic:',e)}finally{loading=false;render()}}
  function tick(){var app=document.querySelector('#app');if(!app||app.classList.contains('hidden')){var h=document.querySelector('#autopilot-actual');if(h)h.style.display='none';return}render();if(!data||Date.now()-last>60000)load()}
  new MutationObserver(function(){setTimeout(tick,0)}).observe(document.documentElement,{subtree:true,childList:true,attributes:true,characterData:true});setInterval(tick,15000);setTimeout(tick,800);
})();
</script>`;

export default {
  async fetch(request, env, ctx) {
    const url = new URL(request.url);
    if (url.pathname === '/health') {
      return new Response(JSON.stringify({
        ok: true,
        service: 'socialscheduler-autopilot',
        version: '7.1',
        ui: 'interactive-actual-state',
        autonomous: true,
        base_worker: 'worker-v6'
      }), {headers: {'content-type':'application/json','cache-control':'no-store'}});
    }
    const response = await base.fetch(request, env, ctx);
    const type = response.headers.get('content-type') || '';
    if (!type.includes('text/html')) return response;
    const html = await response.text();
    const patched = html.includes('</body>') ? html.replace('</body>', PATCH + '</body>') : html + PATCH;
    const headers = new Headers(response.headers);
    headers.set('cache-control','no-store');
    headers.set('content-type','text/html; charset=utf-8');
    return new Response(patched,{status:response.status,statusText:response.statusText,headers});
  }
};
