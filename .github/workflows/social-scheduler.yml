name: Social Scheduler

on:
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: "Scheduler mode"
        required: true
        default: "dry-run"
        type: choice
        options:
          - dry-run
          - live
      content_source:
        description: "Content source"
        required: true
        default: "socialmarket_outbox"
        type: choice
        options:
          - socialmarket_outbox
          - legacy_backlog
  push:
    branches: [main]
    paths:
      - ".github/trigger-buffer"

concurrency:
  group: social-scheduler-publisher
  cancel-in-progress: false

permissions:
  contents: read
  issues: write
  id-token: write

jobs:
  schedule-publisher:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      PUBLISHER_BACKEND: buffer
      BUFFER_API_KEY: ${{ secrets.BUFFER_API_KEY }}
      BUFFER_ORGANIZATION_ID: "68a86463018d512de98d6315"
      SCHEDULER_MODE: ${{ github.event_name == 'workflow_dispatch' && inputs.mode || 'live' }}
      CONTENT_SOURCE: ${{ github.event_name == 'workflow_dispatch' && inputs.content_source || 'socialmarket_outbox' }}
      SOCIALMARKET_OUTBOX_URL: "https://rpfadpdnnxequgvdcfoq.supabase.co/functions/v1/publishing-outbox"
      SOCIALMARKET_OIDC_AUDIENCE: "socialmarket-v2-publishing"
      PUBLISHER_RUNTIME_CONFIG_URL: "https://rpfadpdnnxequgvdcfoq.supabase.co/functions/v1/publisher-runtime-config"
      TRUTH_ARTIFACT_DIR: artifacts
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Load Buffer config from SocialScheduler Admin
        shell: bash
        run: |
          set -euo pipefail
          token_json="$(curl -fsSL -H "Authorization: Bearer ${ACTIONS_ID_TOKEN_REQUEST_TOKEN}" "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=${SOCIALMARKET_OIDC_AUDIENCE}")"
          oidc_token="$(python -c 'import json,sys; print(json.load(sys.stdin)["value"])' <<<"$token_json")"
          runtime_json="$(curl -fsSL -X POST "$PUBLISHER_RUNTIME_CONFIG_URL" \
            -H "Authorization: Bearer ${oidc_token}" \
            -H "Content-Type: application/json" \
            --data '{"provider_key":"buffer"}')"
          export RUNTIME_JSON="$runtime_json"
          python - <<'PY'
          import json, os
          data = json.loads(os.environ["RUNTIME_JSON"])
          env = data.get("env") if data.get("configured") else {}
          env = env if isinstance(env, dict) else {}
          with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as handle:
              for key, value in env.items():
                  if value is None:
                      continue
                  clean = str(value).replace("\r", " ").replace("\n", " ")
                  handle.write(f"{key}={clean}\n")
          print("SocialScheduler Admin Buffer config:", "loaded" if env else "not configured; repository fallback remains active")
          PY

      - name: Require Buffer API secret
        shell: bash
        run: |
          if [ -z "$BUFFER_API_KEY" ]; then
            echo "BUFFER_API_KEY is missing from both SocialScheduler Admin Vault and GitHub Actions secrets."
            exit 1
          fi

      - name: Execute rolling SocialMarket publisher
        shell: bash
        run: |
          mkdir -p artifacts
          set -o pipefail
          python -m src.main --mode "$SCHEDULER_MODE" | tee artifacts/scheduler-result.json

      - name: Sync Buffer feedback metrics
        if: always()
        continue-on-error: true
        shell: bash
        run: |
          mkdir -p artifacts
          python -m scripts.sync_buffer_metrics | tee artifacts/metrics-sync.json
          cat artifacts/metrics-sync.json >> "$GITHUB_STEP_SUMMARY"

      - name: Build Buffer Full Truth Admin Dashboard
        if: always()
        shell: bash
        run: |
          mkdir -p artifacts
          python -m scripts.full_truth_dashboard | tee artifacts/dashboard-build.json
          cat artifacts/admin-dashboard.md >> "$GITHUB_STEP_SUMMARY"

      - name: Update persistent Buffer Admin Dashboard
        if: always() && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          body = Path('artifacts/admin-dashboard.md').read_text(encoding='utf-8')
          Path('artifacts/issue-patch.json').write_text(json.dumps({'body': body}, ensure_ascii=False), encoding='utf-8')
          PY
          gh api --method PATCH "repos/${GITHUB_REPOSITORY}/issues/5" --input artifacts/issue-patch.json

      - name: Upload publisher audit artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: socialscheduler-buffer-${{ github.run_number }}
          path: artifacts/
          if-no-files-found: warn
          retention-days: 30
